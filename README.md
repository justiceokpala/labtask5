method overloading: method overloading is two method having the same name but different parameters..
it can be perfermed by changing the number of arguments or the data type of the arguments.


Method overriding: mehod overriding is a technique that allows the incvoking of functions from another class(base class in the derived class).
we use method overriding if the derived class has the same method as is defined in its base class.
